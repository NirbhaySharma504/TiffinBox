# The conventional Eureka port everyone expects
server.port=8761

spring.application.name=eureka-server

# The Eureka server should NOT register itself as a client of itself.
eureka.client.register-with-eureka=false
# ...and it does not need to pull the registry from anyone it IS the registry.
eureka.client.fetch-registry=false

eureka.instance.hostname=localhost
